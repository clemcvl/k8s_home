---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex
spec:
  destination:
    namespace: plex
    server: https://kubernetes.default.svc
  project: k8shome
  source:
    path: components/plex/chart
    repoURL: https://github.com/clemcvl/k8s_home
    targetRevision: argo_subchart_test
  sync:
    comparedTo:
      destination:
        namespace: plex
        server: https://kubernetes.default.svc
      source:
        path: components/plex/chart
        repoURL: https://github.com/clemcvl/k8s_home
        targetRevision: argo_subchart_test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
